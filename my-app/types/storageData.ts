export type userProfile = {
  name: string;
  email: string;
  password: string;
  birth: string;
};

export type sessionToken = {
  token: string;
};

export type userPreference = {
  theme: string;
  fontSize: number;
};
